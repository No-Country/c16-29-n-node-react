const initialState = {
    
};

function rootReducer(state = initialState, { type, payload }) {
    switch (type) {
        case 1:
            return {
                
            }
        default:
            return state;
    }
}

export default rootReducer;